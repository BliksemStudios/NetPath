import SwiftUI
import SwiftData

@main
struct NetPathApp: App {
    var body: some Scene {
        WindowGroup {
            Text("NetPath — Coming Soon")
                .frame(width: 400, height: 200)
        }
    }
}
